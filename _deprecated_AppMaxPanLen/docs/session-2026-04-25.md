# Development Session - April 25, 2026

## Session Phases

**Phase 1 (Morning):** Complete planning and architecture documentation  
**Phase 2 (Afternoon):** Documentation principles established and applied

## Project: Maximum Panel Length Application

### Session Summary
Complete planning phase for MaxPanelLength application - internal tool for Norex panel specifications lookup. Created comprehensive architecture, API, UI specs, and development plan. Established documentation principles and refactored all specs to follow "concise and highly effective" approach without literal code.

---

## Project Overview

**Purpose:** Quick reference tool for finding maximum panel length based on product specifications.

**Users:** Internal team (St-Hyacinthe and Strathroy plants)

**Input Parameters:**
- Plant Location: STH (St-Hyacinthe) or SRY (Strathroy)
- Panel Color
- Paint Type: PVDF CLASSIC, PVDF SIGNATURE, SMP CLASSIC, SMP PREMIUM, METALLIC
- Gage: 22, 24, 26
- Profile: Grooved, Silkline, Microribbed, None
- Finish: Embossed, Smooth

**Data Sources:**
- MaxLength-STH.csv
- MaxLength-SRY.csv

---

## Architecture Decisions

### Technology Stack
- **Backend:** Python + FastAPI + Uvicorn + Pydantic + pandas
- **Frontend:** React + Vite + Tailwind CSS + Axios
- **Deployment:** Docker + Docker Compose
- **Testing:** pytest (backend), Playwright/Cypress (E2E)

### API Design
**Endpoints:**
- `GET /api/plants` - List plants (STH, SRY)
- `GET /api/plants/{plantId}/data` - Get complete dataset for plant
- `GET /docs` - Swagger UI
- `GET /redoc` - ReDoc

**Key Decision:** Full dataset to frontend (Option A)
- Dataset is small (~50-100 rows, <50KB)
- Single API call provides all data for session
- Enables smart UI filtering (show only valid combinations)
- Frontend calculates results instantly
- Simpler backend (pure data serving, no validation logic)

### Data Strategy
- **Static data:** CSV files represent material physics, rarely change
- **Loading:** Parse CSVs at startup with pandas, hold in memory
- **Full dataset transfer:** Backend serves complete plant data to frontend
- **Client-side processing:** Frontend filters options and calculates results from local dataset
- **Updates:** Replace CSV files and redeploy containers (no hot reload)

### MVP Principles
1. No internationalization (internal tool, display bilingual labels from CSV as-is)
2. NA is valid result (not an error, display clearly to user)
3. Docker Compose for both dev and prod (VM in VPN)
4. Backend unit tests + E2E tests for critical flows
5. No logging infrastructure for MVP
6. Simple deployment: replace CSV → rebuild → restart

---

## UI/UX Design Decisions

### Style Reference
- **Inspiration:** ToitCalc Pro (see References/UI style example.png)
- **Aesthetic:** "Toy but pro" - playful yet professional
- **Layout:** Left panel for parameters, right panel for results
- **Design:** Dark navy header, blue accent buttons, card-based layout, rounded corners

### Interaction Model

**Selection Flow:**
1. **Plant:** Toggle switch (STH ↔ SRY)
2. **Color:** Scrollable list (PRIMARY - selected first)
3. **Other characteristics:** One-click button groups (NOT dropdowns)
   - Paint Type: Button group
   - Gage: Button group (22/24/26)
   - Profile: Button group (Grooved/Silkline/Microribbed/None)
   - Finish: Button group (Embossed/Smooth)

**Dynamic Filtering Logic:**
- User selects Color first from scrollable list
- Paint/Gage/Profile/Finish buttons dynamically enable/disable based on available options for selected color
- When characteristic has zero valid options, show "no options available" visual state
- Each selection cascades to update available options for remaining characteristics

**Result Display:**
- Large prominent number with units (feet)
- Show selected parameters for context
- NA values displayed clearly as "Not Available"

### UI Interaction Model (Detailed)

See [ui-specifications.md](ui-specifications.md) for complete specifications.

**Control Types:**
- **Plant:** Toggle switch (STH ↔ SRY)
- **Color:** Scrollable list (primary selection)
- **Paint/Gage/Profile/Finish:** One-click button groups

**Three-State System:**
- **Selected:** Active/highlighted
- **Unselected (third state):** No selection made
- **No options available:** Disabled/grayed out

**Parameter Hierarchy:**
```
Plant (Level 0)
  ↓
Color (Level 1) - primary filter
  ↓
Paint, Gage, Profile, Finish (Level 2)
```

**Smart State Preservation:**
- **Plant change:** If selected color exists in new plant → keep it, else reset to third state
- **Color change:** If selected characteristics exist for new color → keep them, else reset to third state
- **Clear button:** Reset ALL parameters (including plant) to third state

---

## Files Created/Updated

### Documentation
- ✅ `README.md` - Project overview, structure, reference files
- ✅ `docs/architecture.md` - Architectural decisions and rationale
- ✅ `docs/api-specification.md` - Complete API contracts and endpoint specifications
- ✅ `docs/ui-specifications.md` - Complete UI/UX design and interaction specifications
- ✅ `docs/development-plan.md` - 5-phase development plan with timeline
- ✅ `docs/session-2026-04-25.md` - This session summary

### Folder Structure
```
AppMaxPanLen/
├── README.md
├── docs/
│   ├── architecture.md
│   ├── development-plan.md
│   └── session-2026-04-25.md (this file)
├── References/
│   ├── maximum-panel-length-reference.md
│   ├── norex-panel-length-tables-extracted.md
│   └── UI style example.png
├── src/              (to be created)
│   ├── backend/      (Phase 1)
│   └── frontend/     (Phase 2)
├── MaxLength-STH.csv
├── MaxLength-SRY.csv
└── CALCUL ESPACEMENT ENTREMISES.xlsm
```

---

## Development Plan

### Phase 1: Backend Foundation (1-2 days)
- FastAPI project setup
- CSV parsing with pandas
- Data models (Pydantic)
- API endpoints implementation
- Swagger/ReDoc configuration
- Unit tests (pytest)

### Phase 2: Frontend Foundation (2-3 days)
- React + Vite + Tailwind setup
- Component structure
- ToitCalc Pro styling
- API integration
- State management

### Phase 3: Business Logic (1-2 days)
- Dynamic filter cascading
- Result calculation from local dataset
- NA value handling
- Validation and error handling

### Phase 4: Docker & Deployment (1 day)
- Backend Dockerfile
- Frontend Dockerfile (multi-stage with Nginx)
- docker-compose.yml
- Volume mapping for CSV files
- Deployment scripts

### Phase 5: Testing & Polish (1-2 days)
- Backend test suite completion
- E2E tests for critical flows
- Loading states, transitions
- Documentation updates
- Final polish

**Total Estimate:** 6-10 days

---

## Next Steps

1. **Confirm UI design understanding** ✅
2. **Begin Phase 1:** Backend development
   - Create `src/backend/` structure
   - Initialize FastAPI project
   - Implement CSV parsing
   - Build API endpoints
   - Write unit tests

---

## Key Decisions Log

| Decision | Rationale |
|----------|-----------|
| Full dataset to frontend | Small data (<50KB), enables smart filtering, instant results |
| Client-side filtering | Show only valid combinations based on actual data |
| No calculate endpoint | Frontend has all data, can calculate locally |
| Simple backend | Pure data serving, no business logic |
| Single Docker host | Both containers on same instance with private network |
| Backend not exposed | Only accessible via Nginx proxy, secure internal communication |
| No i18n | Internal tool, bilingual labels already in CSV |
| NA as data value | Part of dataset, not an error condition |
| Docker Compose | Consistent dev/prod environment |
| Python + FastAPI | Fast development, excellent API docs |
| React + Tailwind | Modern stack, rapid UI development |
| One-click controls | Better UX than dropdowns for limited options |
| Color-first selection | Primary filter that determines other options |
| Three-state system | Unselected, selected, no options available |
| Smart state preservation | Keep selections when valid during plant/color changes |
| Clear button resets all | Including plant selection, complete reset |

---

## Questions & Answers

**Q:** Should we use server-side or client-side filtering?  
**A:** Client-side. Data is small/static, provides faster UX.

**Q:** Handle NA values?  
**A:** Display as valid result "Not Available" - user needs this information.

**Q:** Deployment strategy?  
**A:** Docker Compose for both dev (localhost) and prod (VM in VPN).

**Q:** Data update mechanism?  
**A:** Replace CSV files → rebuild containers → restart. No hot reload needed for MVP.

**Q:** Folder naming?  
**A:** `src/` (lowercase), `References/` (for documentation, not application resources).

---

## Status

**Planning Phase:** Complete ✅  
**Documentation Refactoring:** Complete ✅  
**Next Phase:** Phase 1 - Backend Foundation  
**Ready to Start:** Awaiting confirmation to proceed

---

## Session Phase 2: Documentation Principles & Refactoring

### Critical Documentation Insight

**Specifications should describe WHAT and WHY, not HOW**

After discussion about avoiding code duplication and ensuring specs age well:

**✅ What belongs in specifications:**
- Conceptual design patterns: "Extract unique color values from dataset"
- Architectural recommendations: "Use session-level caching for dataset"
- Behavioral descriptions: "Match row using color, paint, and gage"
- Data transformations: "Construct column key by concatenating profile and finish"

**❌ What does NOT belong in specifications:**
- Literal JavaScript/Python code examples
- Specific library usage (Axios, fetch, pandas methods)
- Implementation syntax (useState, map, filter)
- Exact variable names and function signatures

**Rationale:**
- Code changes through refactoring while design patterns remain constant
- Literal code creates duplication (specs + codebase)
- Specs with code become outdated when implementation evolves
- Conceptual guidance stays valid regardless of implementation approach

### Principles Document Created

**File:** `.github/copilot-instructions.md`

**Core principles captured:**
1. **Concise and highly effective** - Question necessity of every section
2. **Avoid duplication** - Never repeat information across documents
3. **Documents should age well** - Focus on concepts that survive refactoring
4. **Specification vs Implementation** - WHAT/WHY not HOW
5. **Separation of concerns** - Each document has distinct role
6. **Project size awareness** - Small projects need lean docs, not enterprise patterns
7. **Practical efficiency** - Docs serve developers, don't burden them

### Refactoring Work Completed

**1. ui-specifications.md** (~600 → 504 lines)
- Removed: "Data Loading & Management" section with 140+ lines of JavaScript code
- Removed: Literal code in "Calculation Logic" section
- Replaced with: Conceptual descriptions of data loading flow, caching strategy, filtering patterns
- Kept: Mapping tables, ASCII diagrams, behavioral specifications, component specs
- Result: No implementation code, pure WHAT/WHY specification

**2. api-specification.md**
- Removed: "Frontend Usage" section (moved conceptual guidance to UI spec)
- API spec now focuses purely on: endpoint contracts, data models, CORS, networking
- No duplication with UI spec

**3. development-plan.md** (107 → 106 lines)
- Fixed: Corrupted timeline section
- Added: Actual timeline breakdown (6-10 days, phase-by-phase)
- Improved: References to specs instead of duplicating component lists
- Cleaned: Success criteria properly separated from timeline

**4. architecture.md**
- Added: Docker networking section (private network, backend not exposed)
- Updated: Deployment environments with dev vs prod networking details

### Docker Networking Documentation

**Added throughout docs:**
- Single Docker host deployment
- Private Docker network for inter-container communication
- Frontend → Backend via `http://backend:8000` (internal network)
- Only frontend port exposed to host (production)
- Backend not accessible from outside (security)
- Nginx proxy handles API routing

---

## Files Status After Refactoring

### Core Documentation
- ✅ `.github/copilot-instructions.md` - **NEW** - Documentation principles
- ✅ `README.md` - Project overview
- ✅ `docs/architecture.md` - WHY decisions (updated with Docker networking)
- ✅ `docs/api-specification.md` - Endpoint contracts (refactored, no frontend usage)
- ✅ `docs/ui-specifications.md` - WHAT UI does (refactored, no literal code)
- ✅ `docs/development-plan.md` - WHEN/phases (cleaned, fixed corruption)
- ✅ `docs/session-2026-04-25.md` - Session recovery (this file)

### Key Metrics
- **Total documentation:** ~1,100 lines (down from ~1,300)
- **Code examples removed:** ~150 lines
- **Duplication eliminated:** Multiple sections
- **Specs now conceptual:** Pure WHAT/WHY, no HOW

---

## Recovery Instructions

**To resume development after reboot:**
1. Read `.github/copilot-instructions.md` for principles
2. Review `docs/architecture.md` for architectural decisions
3. Review `docs/development-plan.md` for phase breakdown
4. Start Phase 1: Backend Foundation (1 day)
   - FastAPI project setup
   - CSV parsing
   - Two endpoints: plants list, plant data
   - Basic tests

**All planning complete. Ready to code.**
